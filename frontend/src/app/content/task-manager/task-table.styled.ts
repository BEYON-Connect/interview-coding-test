import { styled } from "@stitches/react";

// Styled div for task table title using column direction flex
export const TaskTableTitle = styled("div", {
  display: "flex",
  flexDirection: "row",
  gap: "32px",
  paddingBottom: "16px",
  alignItems: "center",
});
